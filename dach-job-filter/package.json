{
  "name": "dach-job-filter",
  "private": true,
  "description": "Arranque unificado: API + Next.js",
  "scripts": {
    "dev": "concurrently -k -n api,web -c cyan,magenta \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "cd backend && py -3.13 -m uvicorn main:app --reload --host 127.0.0.1 --port 8765",
    "dev:web": "cd frontend && npm run dev -- --hostname 127.0.0.1 --port 3000",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
